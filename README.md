# AterBot Bedrock

Bot Bedrock configurado para o servidor definido em `config.json`.

- Host: UCSquat.aternos.me
- Port: 36353
- Username: Bot
- Target version: 1.26.45.1
- Node.js: 20+
- Start command: `npm start`

A versão do protocolo negociada pelo `bedrock-protocol` deve ser compatível com a versão real do servidor. O campo `minecraftVersion` é apenas a versão-alvo da configuração.
